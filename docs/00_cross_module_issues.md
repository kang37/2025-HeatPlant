# 模块间脱节 / 待决问题

三个模块（[数据获取](01_data_acquisition.md) / [CCM](02_ccm.md) / [分类回归](03_classification.md)）
各自往前推进，接口经常没对齐。新对话接手任何一个模块前，先看这里有没有相关条目。

条目按状态分两段：**未解决** 在上，**已解决** 归档在下（不要删除，保留决策记录）。

## 未解决

### 1. S-map 时变系数可能冲击分类模块的判据前提
CCM 模块发现：强系数（|mean_coef|>0.15）方向跨年稳定，近零系数方向逐年反复翻转
70–110 次，均值≈0 实为正负抵消（见 [02_ccm.md](02_ccm.md) "单变量 S-map 有偏"）。
分类模块的六分类（尤其"严格四类"）目前直接用 tp=0..8 上 mean_coef 的符号序列做判据，
没有考虑这层时变噪声。是否需要先过滤掉"近零系数"站再分类，还没有结论。

**2026-09-05 新证据：问题比预期更严重，不止是"近零系数站"的问题**——对"确认
VPD→SIF 方向"134 站（[02_ccm.md](02_ccm.md) 全量验证结果）重做二变量 S-map 后
分层抽样 30 站（Promote/Inhibit/Ambiguous 各10）做稳健性检查：①同一二变量状态
空间，theta 从固定 2 改成逐站 CV 选优，30 站里 10 站方向判断变化——但集中在
基线本来就是 Ambiguous 的站（8/10 翻到确定方向），Promote/Inhibit 基线里各只
翻 1/10，说明"已经判定方向明确"的站对 theta 选择相对稳健；②**状态空间加一维
(温度) 就不稳健**：基线 Promote 的 10 站里 2 站方向变(1 站反转到 Inhibit)，
基线 Inhibit 的 10 站里 8 站方向变、其中 4 站直接反转到 Promote——"已判定
Inhibit"的站，方向判断对"加不加温度进状态空间"极敏感，接近抛硬币。再加一维
(辐射)结果类似(Inhibit 组 6/10 变、3/10 反转)。脚本：
`pipelines/hcsif_buf1000/06_smap_bivar_134.R`（二变量主线）、
`pipelines/hcsif_buf1000/07_smap_robustness_theta_multivar.R`（30站稳健性抽查，
基于旧站点清单，尚未在新清单上重跑）,结果 `data_proc/smap_bivar_134/`。**结论**：
即使限定在"CCM 已确认因果方向"的高置信度子集里，S-map 给出的"促进/抑制"符号本身
对状态空间维度的选择也不稳健，不是只有"近零系数"站才有这个问题。

**2026-09-07 更新**：CCM因果确认判据已统一到
`pipelines/hcsif_buf1000/12_ccm_causal_confirmation.R`(唯一标准，104-345/408等
历史口径全部废弃)，站点数从134变为117。06/08号脚本已用新清单重跑(方向分布变为
Promote 41/Ambiguous 60/Inhibit 16)，但**07号稳健性抽查还没有在新117站上重跑**——
"方向对状态空间维度不稳健"这个结论本身来自旧判据下的134站，新117站是否依然如此
尚未验证，是当前最优先的待办。`recovery_vs_134_direction.R`(生存分析,新117站)
和 `recovery_ar1_pure.R`(连续型φ指标,新117站)已重跑：**离散方向标签(direction_2v)
在换判据前后结论方向一致但显著性变化明显；连续型φ指标对"控不控制VPD"这种建模
选择则几乎不受影响(r=0.999)**——这本身是对"方向标签不稳健"这个问题的又一次独立
佐证，也提示后续分析优先用连续型指标当因变量，而不是离散方向标签。

## 已解决

- ~~重下的因果强度报告版本号变了是否内容有改动~~：2026 年前后核对过，本地
  `file/report/class_report.html` 与重新发布的版本正文逐字一致，只是发布外壳
  （`<!doctype>...<body>`）不同，不需要重新发布，直接在本地文件改。

- ~~CCM 缺零分布/"因果不显著组"判据缺失/2km3km缓冲区扩展/因变量定义甲乙两条路~~
  （原条目2、3、4）：均是针对已删除的 44-60 driver 回归主线(六分类/严格四类)提出
  的问题，**该模块已于 2026-09-07 整体删除**，随模块一起作废，不再适用于当前分析
  设计（现在唯一的因果确认标准见 [03_classification.md](03_classification.md)）。
