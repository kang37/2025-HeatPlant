# HCSIF 站点尺度 SIF 数据处理说明

记录从 HCSIF 原始栅格到站点级 SIF 序列、再到 VPD→SIF 因果分析的完整过程。
最后更新：2026-07-27

---

## 1. 为什么引入 HCSIF

原有分析使用的 SIF 数据是 `data_raw/meteo_stat_SIF_data.csv`，0.05°（约 5.5 km）、8 天、2000–2023，
但项目内**没有任何记录说明它的产品名、版本与提取方法**。这对以 SIF 为因变量的研究是溯源缺口。

HCSIF 的引入解决了两件事：分辨率从 5.5 km 提升到 **500 m**（与站点尺度、30 m 建筑缓冲区的量级更接近），
以及来源完全可考。

**但要注意一个未解决的问题**：HCSIF 的 13 个预测因子中包含 **ERA5-Land 的 VPD**，
而本研究的核心因果链正是 VPD → SIF。也就是说 CCM 检出的因果里，有一部分可能是产品建模时
就放进去的统计关系。同时 **kNDVI 也是 HCSIF 的输入特征之一**，所以"用 kNDVI 交叉验证 SIF"
在 HCSIF 上不构成独立验证。详见第 9 节。

---

## 2. 数据集

| 项目 | 内容 |
|---|---|
| 名称 | HCSIF (High-resolution China SIF) |
| 文献 | Tao S., Chen J.M., Zhang Z. et al. *Scientific Data* 11, 1286 (2024) |
| DOI | 10.57760/sciencedb.16910（ScienceDB V3，CC0） |
| 方法 | 加权堆叠集成模型，将 TROPOMI（3.5×5.6 km）降尺度至 500 m；验证 R²=0.87, RMSE=0.057 |
| 预测因子 | NDVI, NDWI, NIRv, **kNDVI**, LST昼, LST夜, cos(SZA), fPAR, **VPD(ERA5-Land)**, 土壤水分, 坡度, 坡向, DEM |
| 空间分辨率 | 500 m（0.004491576°，即 500 m 的度当量） |
| 时间分辨率 | 8 天，仅生长季 3–10 月 |
| 时间跨度 | 2000–2022 |
| 单位 | mW m⁻² nm⁻¹ sr⁻¹，整型存储，**scale factor = 1e-4** |
| 全量体积 | 607.12 GB / 3754 个文件 |

### 栅格实测参数（`2000196.tif`）

```
维度      10518 × 13712 = 144,222,816 像元
CRS       EPSG:4326 (WGS84 经纬度) —— 不是投影坐标系，无需重投影
范围      lon 73.50016 – 135.0887 ; lat 6.319648 – 53.56205
数据类型  INT4S
NoData    未声明 (NAflag = NaN)   ← 关键，见第 6 节
头部 scale/offset  无 (1/0)        ← 必须手工乘 1e-4
```

`.tif` **自带地理参考**，随附的 `.tfw` 是冗余的（实测移走后 CRS/分辨率/原点仍正确读出）。
所有 `.tfw` 的 MD5 完全相同，说明全部时相共用同一套网格。

---

## 3. 脚本清单与执行顺序

| 顺序 | 脚本 | 作用 |
|---|---|---|
| 1 | `13_hcsif_run.sh` | 流式下载 + 调用提取 + 清理原始栅格 |
| 2 | `13_hcsif_extract.R` | 站点提取（含 `--selftest` 自检模式） |
| 3 | `13_hcsif_add_flag.R` | 追加 `heavy_masked` 标记列 |
| 4 | `13_hcsif_qc.R` | 质检、与 0.05° 旧产品对比 |
| 5 | `13_hcsif_mask_analysis.R` | 掩膜静态/时变判定 |
| 6 | `13_hcsif_grid_shp.R` | 输出提取所用像元的 shapefile |
| 7 | `14_ccm_hcsif_vpd.R` | VPD → SIF 的 CCM 因果检验 |

---

## 4. 下载

### 4.1 获取清单

ScienceDB 的文件列表接口需要登录（未登录返回 `{"code":20001,"message":"用户未登录"}`），
清单由人工登录后从网页「导出文件清单」得到，存为 `data_raw/hcsif/sif_download_link.txt`（3754 行）。

URL 形如：

```
https://download.scidb.cn/download?fileId=<hash>&path=/V3/2000/2000196.tif&fileName=2000196.tif
```

- **下载本身免登录**（实测 HTTP 200），但 `fileId` 是不透明哈希，无法由文件名构造，所以清单不可省
- `traceId` 参数非必需
- 文件名在**查询串**里，不能用 `basename(URL)`——那样只会得到 `download`

### 4.2 只下 `.tif`

清单中 `.tif` 1010 个、`.tfw` 679 个、`.aux.xml` 678 个、`.vat.dbf/.vat.cpg` 各 678/679 个。
由于 `.tif` 自带地理参考且 sidecar 不齐全，**只下载 `.tif`**。

### 4.3 时相筛选

HCSIF 的 DOY 网格是 **60 + 8k**（60, 68, …, 300，每年 31 个），
**不是** MODIS 标准的 1, 9, 17, … 网格。

按「时相起始日落在 5/1–9/30」筛选（DOY 121–273），命中 **DOY 124, 132, …, 268 共 19 个**。
DOY 116 覆盖 4/25–5/2，起始日在 4 月，故排除。

实际结果：435 个时相。2000 年 18 个（源数据缺 DOY 220）、2022 年 18 个（源数据缺 DOY 140）、其余各 19 个。
体积约 **44 GB**（每景 85–115 MB），仅为全量 607 GB 的 7%。

### 4.4 流式设计

本机可用磁盘一度只剩 15–32 GB，因此采用「逐年下载 → 提取 → 立即删除栅格」，峰值占用约 2 GB。

**下载失败必须设完整性闸门**：2002 年首次运行时 19 个时相只到位 4 个（服务端在持续大流量后限流断连，
`curl --retry` 兜不住连接级失败），而脚本当时仍继续提取并删除了原始文件，
生成了一个看似正常、实则只有 4 个时相的年度文件。2001 年同样丢了 2 个。

现已加入：3 轮重试 + 轮间 60 秒退避 + 文件大小下限校验（<10 MB 判为损坏）+
**到位数不足则跳过提取并保留已下载文件**。

---

## 5. 提取

### 5.1 站点

924 个气象站，坐标取自既有的 `meteo_stat_SIF_data.csv`，存为 `data_raw/hcsif/stations_924.csv`。
经度 75.95–134.02，纬度 18.50–50.40，无缺失坐标。

### 5.2 三种取值方案

| 列名 | 含义 |
|---|---|
| `SIF` | 站点所在的**单个 500 m 像元**值 |
| `SIF_buf750` | 站点周围 **750 m** 半径内所有未掩膜像元的均值（约 3×3 像元） |
| `SIF_buf1000` | 站点周围 **1000 m** 半径内所有未掩膜像元的均值（约 5×5 像元） |

**先剔除掩膜值再求均值**是必要的——直接 `mean()` 会把掩膜的 0 当成真实低值把均值拉低。

配套列：`n_valid750` / `n_valid1000` 记录参与平均的像元数；`n_valid` 是 `n_valid750` 的向后兼容别名。

### 5.3 实现要点

- **投影**：站点为 WGS84，栅格也是 EPSG:4326，本例无需重投影；但脚本仍保留通用的投影分支，
  因为若栅格是正弦/等积投影而不先投影，会静默取到错误像元
- **scale factor**：先查文件头的 scale/offset（若 terra 已自动应用则不再乘第二次），
  HCSIF 头部无 scale，故手工乘 1e-4
- **NaN vs NA**：`mean(c(NA,NA), na.rm=TRUE)` 返回 **NaN 而非 NA**，必须显式转换，否则下游行为不一致
- **物理值域**：缩放后落在 [-1, 5] 之外判为填充值置 NA

### 5.4 取值网格的矢量文件

`13_hcsif_grid_shp.R` 输出各方案实际取值所用的像元多边形，便于在 GIS 中核对：

| 文件 | 内容 | 多边形数 |
|---|---|---|
| `hcsif_grid_point.shp` | 每站 1 个 —— 站点落入的那个 500 m 像元 | 924 |
| `hcsif_grid_buf750.shp` | 750 m 缓冲区覆盖的像元 | 7,881（平均每站 8.5 个） |
| `hcsif_grid_buf1000.shp` | 1000 m 缓冲区覆盖的像元 | 13,973（平均每站 15.1 个） |

点值图层附带站点级统计（`n_obs` / `n_valid` / `n_miss` / `miss_pct` / `n_masked` / `sif_mean` 等）。

网格几何优先从现存栅格读取；原始栅格被流式流程删除后，回退到实测确认的内置参数
（`res = 0.0044915764`，左上角像元中心 `73.5024023349, 53.5598030274`，10518 × 13712），
两条路径结果一致。**所以补出任意半径的网格都不需要重新下载栅格。**

> 注意：**12 个站点与其他站点共用同一个 500 m 像元**（站点间距 < 500 m），
> 这些站点的点值 SIF 必然完全相同，做站点级统计时需留意非独立性。

### 5.5 自检

`Rscript 13_hcsif_extract.R --selftest` 用合成的 Albers 投影整型栅格验证全流程：
时相过滤、投影+scale 还原（最大误差 0.00012）、日期换算、0 值掩膜识别、列结构。

---

## 6. 掩膜与缺失值（核心问题）

### 6.1 0 是掩膜，不是观测

全图 **68.8% 的像元恰好等于 0**，而栅格**没有声明 NoData**，所以 terra 会把 0 当作合法数值读入。

判据：7 月中旬不存在真实的零光合；原始数据是整型，真实的近零信号会是 3、−12 这类小整数而非恰好 0；
且站点自己的像元为 0 时，周围像元均值往往正常（例：站点 50527 点值 0，750 m 邻域 12 个像元均值 0.161）。

处理：精确等于 0 判为掩膜，置 NA，并用 `pt_masked` 列标记。

### 6.2 掩膜是时变的，不是静态的

全 23 年、435 个时相统计：

| | 站点数 |
|---|---|
| 从不被掩膜 | 23 |
| 有时被掩膜 | 901 |
| 始终被掩膜 | 0 |

相邻时相掩膜集合的 Jaccard 相似度：均值 0.800，最小 0.021，最大 1.000（静态应恒为 1.000）。

**季节性明显**：5 月初 DOY 124 掩膜率 26.5% → 盛夏 DOY 204 降至 16.6% → 9 月底 DOY 268 回升至 24.7%。
这是物候信号——生长季两端植被少，更多像元低于植被阈值。

> 注意：早期基于 2000–2003 子集的统计（"402 站从不被掩膜"）不适用于全量数据。
> 时相一多，几乎每个站都至少被掩膜过一次，二分分组随之失去意义。

### 6.3 缺失与城市化正相关

按建成度（30 m 缓冲区建筑占地）四分位：

| 分位 | n | 建筑占地中位数 (m²) | 掩膜时相占比中位数 |
|---|---|---|---|
| Q1 | 170 | 0.0 | 3.2% |
| Q2 | 170 | 0.0 | 5.7% |
| Q3 | 169 | 343.2 | 7.1% |
| Q4 | 170 | 935.5 | **13.9%** |

Spearman(掩膜比例, 建筑占地) = **0.157, p = 3.95e-05**；
Spearman(全掩膜比例, 建筑占地) = 0.118, p = 0.00204。

关系真实且单调，但相关强度中等偏弱。**这是系统性选择偏倚**：缺失最多的恰好是城市化程度最高的站点，
而这正是本研究关注的对象。

### 6.4 总体缺失率

```
点值 SIF        有效 79.4%   缺失 20.6%
邻域 SIF_buf750 有效 90.5%   缺失  9.5%
两者皆缺 (n_valid == 0)      9.45%  (37,993 / 401,940 条)
```

缓冲区全部像元都被掩膜时，`SIF_buf750 = NA`、`n_valid = 0`；此时点值必然也缺失
（中心像元就在缓冲区内）。

### 6.5 heavy_masked 标记

**37 个站点**在超过 50% 的时相上两者皆缺，由 `13_hcsif_add_flag.R` 标记为 `heavy_masked = TRUE`，
并另存 `station_mask_flag.csv`。

这批站点集中在**大城市**而非干旱区（北京 4 个站、石家庄、邢台、济南、青岛、南宁等；
西部经度<105 的重度站占 4.9%、东部占 3.9%，基本无差异）。

### 6.6 5 个时相完全没有掩膜

2001 DOY 164、2001 DOY 172、2002 DOY 140、2012 DOY 196、2012 DOY 204 —— 这 5 景（5/435 = 1.1%）
不含任何 0 值。同一批城市站点在这些时相会拿到真实值，其余时相是 0，
导致站点序列在时间上不自洽。**尚未处理**，见第 9 节。

---

## 7. 质检结果

### 7.1 与 0.05° 旧产品对比

```
HCSIF  均值 0.3414  中位 0.3568  sd 0.1168  范围 [0.0024, 0.8717]
旧产品 均值 0.1675
比值中位数 2.079            ← HCSIF 系统性约为旧产品的 2 倍
全样本 Pearson r = 0.566
站点级 r 中位数 0.481（10 分位 −0.020，最低 −0.482）
Spearman(建成度, 站点级一致性) = −0.113, p = 0.0035
```

两点保留意见：

1. 两者标称单位相同（mW m⁻² nm⁻¹ sr⁻¹），**2 倍的系统性差异需要解释**。
   对 CCM 无影响（去趋势后倍数会消掉），但写方法学时要交代
2. 建成度与一致性的负相关方向符合"500 m 分辨出了粗产品丢失的城市信号"的预期，
   但 rho 仅 −0.113，**不足以支撑这个论断**——大部分分歧另有来源

### 7.2 三种取值方案的关系

| 方案 | 有效率 | 参与平均的像元数（中位） |
|---|---|---|
| `SIF` 点值 | 79.4% | 1 |
| `SIF_buf750` | 90.5% | 8 |
| `SIF_buf1000` | **92.7%** | 14 |

三者数值高度一致：`r(750, 1000) = 0.9930`（中位差 −0.001），`r(点值, 1000) = 0.9587`，
`r(点值, 750) = 0.974`。也就是说**扩大缓冲区几乎不损失空间特异性，纯赚覆盖率**。

1000 m 相对 750 m 多救回 8,482 条（2.11%），但仍有 7.34% 的记录三种方案全缺。

### 7.3 1000 m 对 heavy_masked 站点的补救效果

在 37 个 `heavy_masked` 站点（多为大城市）上，扩大半径的收益明显大于整体：

| 方案 | 这批站点上的有效率 |
|---|---|
| `SIF` 点值 | 21.4% |
| `SIF_buf750` | 33.0% |
| `SIF_buf1000` | **44.3%** |

1 km 把可用数据量相对点值翻了一倍多。**但 44.3% 仍然偏低**，
这批站点即便用 1 km 邻域也难以支撑可靠的时间序列分析，建议在敏感性分析中单独处理或剔除。

### 7.4 复现性验证

v2 重跑（重新下载全部 435 景、重新提取）与 v1 逐位比对：

```
SIF        完全相同 401940 / 401940  (100.0000%)
SIF_buf750 完全相同 401940 / 401940  (100.0000%)
n_valid    完全相同 401940 / 401940  (100.0000%)
```

整条下载—提取链**完全确定性**，无隐藏的非确定性来源。

---

## 8. CCM 分析

### 8.1 与既有 `01_ccm_vpd.R` 保持可比的部分

- VPD 定义相同：Tetens 公式 `svp = 0.6112·exp(17.67·T/(T+243.5))`，`vpd = svp − rh/100·svp`，
  QC 为 `vpd < 0 或 > 10 → NA`
- 热胁迫阈值同为 2.0 kPa
- 同样的线性去趋势、`EmbedDimension` 选 E、`CCM(Tp=0)` + 收敛趋势、`SMap(theta=2)` 取效应方向
- 同样的滞后扫描 tp = 0…8

### 8.2 必要的差异

1. **时间步长**由「周」改为 HCSIF 的 8 天窗口，VPD 按同窗口聚合（窗口内至少 5 天有效气象数据）
2. **因变量跑两套**：`SIF`（点值）与 `SIF_buf750`（邻域均值）
3. **滞后在年内进行**，不跨越 10 月–次年 4 月的空档

### 8.3 缺失值处理（关键）

**不能删除缺失行后重新编号。** CCM 靠时间延迟嵌入重建吸引子，相邻性是它唯一的信息来源；
删行重编号会把不相邻的观测伪造成相邻的。

合成实验（已知因果的耦合 logistic 系统，20% 成片缺失，12 次重复）：

| 策略 | rho 均值 | 平均偏差 | 绝对偏差 |
|---|---|---|---|
| 基线（无缺失） | 0.480 | — | — |
| **保留 NA、时间轴不动** ✅ | 0.485 | **+0.005** | 0.063 |
| 删除缺失行 + 重新编号 ❌ | 0.412 | **−0.069** | 0.080 |
| 短缺口线性插补（≤2 步） | 0.460 | −0.020 | 0.062 |

删行重编号**系统性低估 rho 约 0.069（相对 14%），方向固定**。

现行做法：铺一条完整的 8 天规则时间轴（`idx8` = 距 2000-01-01 的 8 天绝对序号），
观测放在真实位置，其余留 NA。跨越 NA 的嵌入自然失效，
同时让每年 10 月–次年 4 月的休眠期表现为真实空档。

配套：`maxE` 与 `libSizes` 按**实际有效配对数**计算而非补齐后的序列长度，
否则 NA 占位会把嵌入维度和文库规模撑虚；去趋势也用 `idx8` 而非行号。

### 8.4 结果

输出目录 **`data_proc/ccm_hcsif_500m/`**（独立目录，不触碰任何既有结果）：

| 文件 | 内容 |
|---|---|
| `ccm_hcsif_vpd_<时间戳>.rds` / `.csv` | 主结果（含坐标） |
| `ccm_hcsif_vpd_<时间戳>_raw.rds` | 未合并元数据的原始结果 |
| `parts/part_NN.rds` | 每个组合的检查点，支持断点续算 |
| `qc/` | 质检输出 |
| `ccm_full_run_clean.log` | 运行日志（已滤除 rEDM 的 NaN 警告） |

最近一次完整运行：901 站、16,036 行、36 个组合、493 分钟。

结果速览（`vpd_mean` 为 X）：

| y | tp=0 | tp=1 | tp=2 | tp=4 | tp=8 |
|---|---|---|---|---|---|
| `SIF_buf750` | 0.062 | 0.045 | 0.038 | 0.030 | 0.003 |
| `SIF`（点值） | 0.040 | 0.031 | 0.026 | 0.017 | 0.000 |

rho 随滞后单调衰减，符合物理直觉；邻域均值一致地高于点值。

> `heat_over_sum`（累积 VPD 超量）在最近一次运行中被有意关闭
> （脚本注释：「热胁迫驱动本次不算」）。早期 25 站试跑显示它的信号
> **远强于** `vpd_mean`（tp=0 时 rho 0.218 vs 0.073），如需要可重新打开。

---

## 9. 已知问题与待决事项

| # | 问题 | 状态 |
|---|---|---|
| 1 | **HCSIF 用 ERA5-Land VPD 作为预测因子**，与本研究 VPD→SIF 的因果链存在循环论证风险 | **未解决**。建议配一个无气象输入的产品（如 CSIF，纯 MODIS 反射率+神经网络）做交叉验证 |
| 2 | kNDVI 是 HCSIF 的输入特征，无法作为独立验证变量 | 同上 |
| 3 | 5/435 个时相无掩膜，站点序列时间上不自洽 | 已识别未处理 |
| 4 | 缺失率随季节系统变化，而季节与 VPD 强相关（informative missingness） | 已识别。当前靠"保留 NA"缓解，但未做敏感性分析 |
| 5 | HCSIF 约为旧产品 2 倍，原因不明 | 待查 |
| 6 | 37 个 heavy_masked 站点（多为大城市）数据质量差 | 已标记。1 km 缓冲区把有效率从 21.4% 提到 44.3%，仍偏低，待决定是否剔除 |
| 7 | CCM 未做 surrogate 检验，共同季节性可能导致假阳性 | 未做 |

关于 #4 的建议：**CCM 以 `SIF_buf750` 为主变量**（90.5% 完整，缺失的季节性弱得多），
点值作敏感性检验。7.2 节已证明这几乎不损失空间特异性。

---

## 10. 复现

```bash
# 全流程（需先有 sif_download_link.txt）
./13_hcsif_run.sh all

# 单年
./13_hcsif_run.sh 2015

# 换输出目录（如生成含 1 km 缓冲区的 v2）
HCSIF_OUT_DIR=data_raw/hcsif/station_v2 ./13_hcsif_run.sh all

# 提取逻辑自检
Rscript 13_hcsif_extract.R --selftest

# 后处理
Rscript 13_hcsif_add_flag.R
Rscript 13_hcsif_qc.R
Rscript 13_hcsif_mask_analysis.R
Rscript 13_hcsif_grid_shp.R

# CCM（--stations N 可抽样试跑）
Rscript 14_ccm_hcsif_vpd.R
```

### 数据目录

```
data_raw/hcsif/
├── sif_download_link.txt      下载清单(3754 行，人工登录导出)
├── stations_924.csv           站点坐标
├── tmp/                       流式下载暂存(处理后自动清空)
├── station/                   提取结果 v1: SIF + SIF_buf750
├── station_v2/                提取结果 v2: 增加 SIF_buf1000
├── vpd_8day_cache.rds         8 天窗口 VPD 缓存
└── run_all.log / run_v2.log   下载日志
```

年度文件 `hcsif_station_<年份>.csv` 的列：

```
meteo_stat, latitude, longitude, year, month, day, doy, date,
SIF, SIF_raw, pt_masked,
SIF_buf750, n_valid750, [SIF_buf1000, n_valid1000,]  n_valid,
src_file, src_crs, scale_note, heavy_masked
```
